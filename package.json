{
  "name": "rutina-gluteo",
  "version": "1.0.0",
  "private": true,
  "description": "App de rutina de entrenamiento con registro de series y cronometro de descanso",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev:frontend": "npm run start --prefix frontend"
  },
  "engines": {
    "node": "^22.22.3 || ^24.15.0 || >=26.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
